{
  "buildCommand": "npm run build --workspace=contract && npm run build --workspace=frontend",
  "outputDirectory": "frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
